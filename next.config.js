/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "i.imgur.com",
            "www.midjourney.com"
        ]
    }
}

module.exports = nextConfig
